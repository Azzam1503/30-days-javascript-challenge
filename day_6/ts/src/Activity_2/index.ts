//Task 3 Use the push method to add a new number to the end of the array and log the updated array.
const nums: number[] = [2, 3, 4, 1, 3, 4];
nums.push(5);
console.log(nums);

console.log("-----------------------------------");

// Task 4 Use the pop method to add a new number to the end of the array and log the updated array.

nums.pop();
console.log(nums)

console.log("-----------------------------------");

// Use the shift method to remove the last element from the array and log the updated array.
nums.shift()
console.log(nums);

console.log("-----------------------------------");

// Use the unshift method to add a new number to the begining of the array and log the updated array. 
nums.unshift(25);
console.log(nums);